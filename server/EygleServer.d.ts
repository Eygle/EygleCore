export declare class EygleServer {
    /**
     * Initialize first (root)
     * @param {string} root
     * @param config
     * @return {any}
     */
    static init(root: string, config: any): any;
}
